import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  okButton: {
    width: 100,
    marginBottom: 10,
    alignSelf: 'center',
  },
});

export default styles;
